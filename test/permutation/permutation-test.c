#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "../../src/permutation/permutation.h"

int main (void) {

/*	vector *v;
	init_vector(v);

	add_vector(v, "Peter");
	add_vector(v, "Hans");

	print_vector(v);
*/	permute("%s.%d.%s");
	return EXIT_SUCCESS;
	//return EXIT_FAILURE;
}

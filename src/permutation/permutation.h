#ifndef PERMUTATION_H_
#define PERMUTATION_H_

#include "../../src/input/input.h"
//void permute(char*, vector*);
void permute(char*);
int count_string(char*, char*);
void store_numbers_in_vector(vector*);
void store_words_in_vector(vector*, vector*);

#endif
